# Plantilla de credenciales de firma para release.
# 1. Copiá este archivo a "keystore.properties" (en la raíz del proyecto).
# 2. Generá el keystore (ver instrucciones) y completá los valores reales.
# 3. NUNCA subas keystore.properties ni el .jks a git.

storeFile=cletaeats-key.jks
storePassword=TU_CONTRASENA_DEL_KEYSTORE
keyAlias=cletaeats
keyPassword=TU_CONTRASENA_DE_LA_LLAVE
